import { auth } from '@clerk/nextjs';
import React from 'react'


function Publish() {

  // const { userId } : { userId: string | null } = auth();  // console.log(userId);
  // console.log(userId);
  
  return (
    <div className='py-20'>Publish</div>
  )
}

export default Publish